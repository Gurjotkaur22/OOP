/**
 * Constants header
 */

// Specify dashes width
static int DASHES_WIDTH = 60;

// Specify password filename
static string PASSWORD_FILENAME = "password.txt";

// Specify students filename
static string STUDENTS_FILENAME = "students.txt";

// Specify plot width
static int PLOT_WIDTH = 10;

// Specify passing score
static int PASSING_SCORE = 7;
